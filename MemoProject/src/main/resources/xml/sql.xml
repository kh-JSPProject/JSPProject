<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>

<entry key="userSelect">
	select user_no, user_pw, user_name
	from tb_users
	where user_id = ?
</entry>

<entry key="memoListSelect">
	SELECT memo_no, user_no, title, content, 
		   to_char(reg_date,'YYYY-MM-DD HH:MI') reg_date,
		   to_char(update_date,'YYYY-MM-DD HH:MI')
	FROM tb_memos
	WHERE user_no = (SELECT user_no
					 FROM TB_USERS
					 WHERE user_id = ?)
	ORDER BY memo_no DESC
</entry>
	
</properties>